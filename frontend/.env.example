VITE_API_URL=http://127.0.0.1:8000
VITE_API_KEY=your-demo-access-key

# VITE_API_KEY is bundled into the browser. Use it only as a portfolio demo
# access gate, not as secure production authentication.
